services:
  - type: web
    name: Aarogyaai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend.app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
      - key: JWT_SECRET
        value: replace-this-secret-with-env-var
